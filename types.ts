export interface TimeState {
  hours: string;
  minutes: string;
  seconds: string;
}

export interface CalculationResult {
  hours: number;
  minutes: number;
  seconds: number;
  totalSeconds: number;
}
